#pragma once
#include <iostream>
#include <fstream>
#include <vector>
#include <cstdlib>
#include <math.h>
using namespace std;

double moyenneVect(vector<double> vecteurInit);

double varianceVect(vector<double> vecteurInit);

double CalcScore(vector<double> vecteurInit, double valeur);
